---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pcfnorm/rootfs
    tag: 1.0.28


inputs:
- name: buildpack
- name: pcf-pipelines

params:
  CF_API_URI:
  CF_USERNAME:
  CF_PASSWORD:
  BUILDPACK_NAME:
  STACKS:

run:
  path: pcf-pipelines/tasks/stage-buildpack/task.sh
